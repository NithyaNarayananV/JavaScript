showPrime(20);

function showPrime(limit){
    for (i=2;i<=limit;i++){
        flag=1;
        for(j=2;j<=i/2;j++){
            if (i%j==0){
                flag=0;
                break;
            }
        }
        if (flag===1) console.log(i);
    }
}
