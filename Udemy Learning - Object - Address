const address = {
    street : '1st Street',
    city : 'Chennai',
    zipCode : 600040,
    
};
function showAddress (address){
    for(let k in address){
        console.log(k+' : '+address[k]);
    }        
}
showAddress(address);
